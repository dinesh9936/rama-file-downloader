<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">


    <application>

        <receiver
            android:name=".DownloadActionReceiver"
            android:exported="false">

            <intent-filter>
                <action android:name="downloader.pause" />
                <action android:name="downloader.cancel" />
            </intent-filter>

        </receiver>

        <service
        android:name=".DownloaderForegroundService"
        android:foregroundServiceType="dataSync"
        android:exported="false" />
    </application>
</manifest>